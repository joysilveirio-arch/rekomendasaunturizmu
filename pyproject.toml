[project]
name = "rekomendasaunturizmu"
version = "0.1.0"
description = "Timor Leste Tourism Recommendation System"
requires-python = ">=3.11"

dependencies = [
    "fastapi",
    "uvicorn",
    "sqlalchemy",
    "pydantic",
    "python-dotenv"
]

[tool.vercel]
entrypoint = "backend.main:app"